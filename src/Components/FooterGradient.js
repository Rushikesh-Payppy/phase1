import React from "react";
import "@/Styles/flix/flix.css";

const FooterGradient = ({ positionValue }) => {
  return (
    <div className={`${positionValue} bottom-0 left-0 footer-component-gradient w-full h-24`}></div>
  );
};

export default FooterGradient;
